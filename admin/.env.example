# OA Agent 後台（admin）前端環境變數範本
# 用法：複製成 admin/.env.local 後填值（.env.local 已被 .gitignore 排除，不會進 repo）。
# 注意：Vite 變數是「build 階段」bake 進 bundle，不是 runtime 讀。改值要重新 build / 重新部署才生效。
# Zeabur 部署時「不要」靠 .env 檔，請改在 admin 服務的 Variables 設定（會當成 build ARG 傳入 Dockerfile.admin）。

# 後端 API 來源網域（前後端分開部署）。
# 本機開發請「留空或不要設」→ 走相對路徑，由 vite.config.ts 的 proxy 代理到 localhost:3000。
# Zeabur 部署（admin 服務）設成 server 服務的對外網域，例：
# VITE_API_BASE=https://oa-agent-server.zeabur.app
VITE_API_BASE=

# 外觀預覽 iframe 載入的 widget client（聊天前端）來源網域。
# 本機開發請「留空或不要設」→ fallback 到 http://localhost:5173。
# 公開部署「務必設定」，否則公開後台的預覽 iframe 會連到 localhost（私有網段）被瀏覽器封鎖。
# 值是 client（widget）服務的對外網域，例：
# VITE_WIDGET_ORIGIN=https://oa-agent.zeabur.app
VITE_WIDGET_ORIGIN=

# 「外觀」分頁「開啟示範網站」按鈕連到的外部網址。
# 留空 → 用程式內建預設（presale demo 部署網址）。要改成自己的 demo 網址時設定，例：
# VITE_DEMO_SITE_URL=https://oa-agent-presale-tenant.zeabur.app/
VITE_DEMO_SITE_URL=
