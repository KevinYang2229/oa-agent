# 模擬租戶網站設定（純前端嵌入版；複製成 .env 後修改，server.mjs 會自動載入同目錄 .env）
# 注意：這版不做 SSO，沒有 ssoSecret、不簽 token，後端只是靜態檔伺服器。

# 此模擬網站的埠（用 4001 以免和 widge-tenant 的 4000 衝突）
PORT=4001

# OA Agent 來源
OA_ORIGIN=http://localhost:3000

# 租戶「公開」金鑰（pk 可放前端；建租戶時取得）
TENANT_PK=pk_3dc8a9721d103efb0972aac2c0ef79d237698f765178fc6a
